# Command Evidence

Store terminal transcripts, version checks, install logs, test output, and failure output here.

Use stable file names such as:

```text
YYYY-MM-DD_tool-slug_command-purpose.txt
```
